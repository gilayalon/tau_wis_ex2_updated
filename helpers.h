#ifndef HELPERS_H_
#define HELPERS_H_

#include <string.h>
#include <stdlib.h>
#include <ctype.h>
#include <limits.h>

int strToInt(char *);
int isEmpty(char *);
char *ltrim(char *);
char *rtrim(char *);
char *trim(char *);

#endif
